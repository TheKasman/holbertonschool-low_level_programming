#include <stdio.h>
#include "main.h"

int mul(int, int);

/**
 * mul - multiplies two intervals together
 * @a: first value
 * @b: second value
 * Return: the result of multiplying them together (result)
 */

int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return (sum);
}
